using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;
using Moq;
using System.Threading;
using System.Threading.Tasks;
using Tuhu.Service.Promotion.DataAccess;
using Tuhu.Service.Promotion.Server;
using Xunit;

namespace Tuhu.Service.Promotion.UnitTest
{
    public class PromotionRepositoryTest
    {
        //[Fact]
        //public async Task DemoMethod()
        //{
        //    var repository = new PromotionCodeRepository(new Mock<IDbHelperFactory>().Object);

        //    Assert.True(await repository.DemoMethodAsync(1, It.IsAny<CancellationToken>()));
        //    Assert.True(await repository.DemoMethodAsync(-1, It.IsAny<CancellationToken>()));
        //    Assert.False(await repository.DemoMethodAsync(0, It.IsAny<CancellationToken>()));
        //}
    }
}
